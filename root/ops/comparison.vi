
use derive::{Enum, Struct, Tuple, Variant};
use ops::arithmetic::Neg;

pub trait[T] Eq {
  #[builtin = "eq"]
  fn eq(a: &T, b: &T) -> Bool;

  #[builtin = "ne"]
  fn ne(a: &T, b: &T) -> Bool;
}

pub mod Eq {
  pub impl unary[T; Eq[T]]: Eq[(T,)] {
    fn eq(&(a: T,), &(b: T,)) -> Bool {
      a == b
    }

    fn ne(&(a: T,), &(b: T,)) -> Bool {
      a != b
    }
  }

  pub impl tuple[T, I, R; Tuple[T, I, R], Eq[I], Eq[R]]: Eq[T] {
    fn eq(&a: &T, &b: &T) -> Bool {
      let &(ai, ar) = &a as &(I, R);
      let &(bi, br) = &b as &(I, R);
      ai == bi and ar == br
    }

    fn ne(&a: &T, &b: &T) -> Bool {
      let &(ai, ar) = &a as &(I, R);
      let &(bi, br) = &b as &(I, R);
      ai != bi or ar != br
    }
  }

  pub impl ref[T; Eq[T]]: Eq[&T] {
    fn eq(&&a: &&T, &&b: &&T) -> Bool {
      a == b
    }

    fn ne(&&a: &&T, &&b: &&T) -> Bool {
      a != b
    }
  }

  #[basic]
  pub impl struct_[S, C; Struct[S, C], Eq[C]]: Eq[S] {
    fn eq(&a: &S, &b: &S) -> Bool {
      &a as &C == &b as &C
    }

    fn ne(&a: &S, &b: &S) -> Bool {
      &a as &C != &b as &C
    }
  }

  pub impl never: Eq[!] {
    fn eq(&_: &!, &_: &!) -> Bool {
      true
    }

    fn ne(&_: &!, &_: &!) -> Bool {
      false
    }
  }

  // We don't have a blanket implementation of Eq for Unit to prevent unwanted invocations.
  // To support enums with unit variants, we implement it manually.
  pub impl unit_variant[R; Eq[R]]: Eq[Variant[(), R]] {
    fn eq(&a: &Variant[(), R], &b: &Variant[(), R]) -> Bool {
      match &(a, b) {
        &(Variant::Init(_), Variant::Init(_)) { true }
        &(Variant::Rest(a_rest), Variant::Rest(b_rest)) { a_rest == b_rest }
        _ { false }
      }
    }

    fn ne(&a: &Variant[(), R], &b: &Variant[(), R]) -> Bool {
      match &(a, b) {
        &(Variant::Init(_), Variant::Init(_)) { false }
        &(Variant::Rest(a_rest), Variant::Rest(b_rest)) { a_rest != b_rest }
        _ { true }
      }
    }
  }

  pub impl variant[I, R; Eq[I], Eq[R]]: Eq[Variant[I, R]] {
    fn eq(&a: &Variant[I, R], &b: &Variant[I, R]) -> Bool {
      match &(a, b) {
        &(Variant::Init(a_init), Variant::Init(b_init)) { a_init == b_init }
        &(Variant::Rest(a_rest), Variant::Rest(b_rest)) { a_rest == b_rest }
        _ { false }
      }
    }

    fn ne(&a: &Variant[I, R], &b: &Variant[I, R]) -> Bool {
      match &(a, b) {
        &(Variant::Init(a_init), Variant::Init(b_init)) { a_init != b_init }
        &(Variant::Rest(a_rest), Variant::Rest(b_rest)) { a_rest != b_rest }
        _ { true }
      }
    }
  }

  #[basic]
  pub impl enum_[E, V; Enum[E, V], Eq[V]]: Eq[E] {
    fn eq(&a: &E, &b: &E) -> Bool {
      Enum::match_ref(
        &a,
        fn (&a_variant: &V) { Enum::match_ref(&b, fn (&b_variant: &V) { a_variant == b_variant }) },
      )
    }

    fn ne(&a: &E, &b: &E) -> Bool {
      Enum::match_ref(
        &a,
        fn (&a_variant: &V) { Enum::match_ref(&b, fn (&b_variant: &V) { a_variant != b_variant }) },
      )
    }
  }
}

pub trait[T] Ord {
  fn .cmp(a: &T, b: &T) -> Ord;
  fn lt(a: &T, b: &T) -> Bool;
  fn le(a: &T, b: &T) -> Bool;
}

#[impl(Show)]
pub enum* Ord {
  Lt(),
  Eq(),
  Gt(),
}

pub mod Ord {
  pub impl : Neg[Ord, Ord] {
    fn neg(a: Ord) -> Ord {
      match a {
        Ord::Lt() { Ord::Gt() }
        Ord::Eq() { Ord::Eq() }
        Ord::Gt() { Ord::Lt() }
      }
    }
  }

  pub impl unary[T; Ord[T]]: Ord[(T,)] {
    fn cmp(&(a: T,), &(b: T,)) -> Ord {
      Ord::cmp(&a, &b)
    }

    fn lt(&(a: T,), &(b: T,)) -> Bool {
      a < b
    }

    fn le(&(a: T,), &(b: T,)) -> Bool {
      a <= b
    }
  }

  pub impl tuple[T, I, R; Tuple[T, I, R], Ord[I], Ord[R]]: Ord[T] {
    fn cmp(&a: &T, &b: &T) -> Ord {
      let &(ai, ar) = &a as &(I, R);
      let &(bi, br) = &b as &(I, R);
      match ai.cmp(&bi) {
        Ord::Lt() { Ord::Lt() }
        Ord::Gt() { Ord::Gt() }
        Ord::Eq() { ar.cmp(&br) }
      }
    }

    fn lt(a: &T, b: &T) -> Bool {
      Ord::lt_from_cmp(a, b)
    }

    fn le(a: &T, b: &T) -> Bool {
      Ord::le_from_cmp(a, b)
    }
  }

  pub impl ref[T; Ord[T]]: Ord[&T] {
    fn cmp(&&a: &&T, &&b: &&T) -> Ord {
      Ord::cmp(&a, &b)
    }

    fn lt(&&a: &&T, &&b: &&T) -> Bool {
      a < b
    }

    fn le(&&a: &&T, &&b: &&T) -> Bool {
      a <= b
    }
  }

  #[basic]
  pub impl struct_[S, C; Struct[S, C], Ord[C]]: Ord[S] {
    fn cmp(&a: &S, &b: &S) -> Ord {
      Ord::cmp(&a as &C, &b as &C)
    }

    fn lt(&a: &S, &b: &S) -> Bool {
      &a as &C < &b as &C
    }

    fn le(&a: &S, &b: &S) -> Bool {
      &a as &C <= &b as &C
    }
  }

  pub fn gt[T; Ord[T]](&a: &T, &b: &T) -> Bool {
    b < a
  }

  pub fn ge[T; Ord[T]](&a: &T, &b: &T) -> Bool {
    b <= a
  }

  pub fn lt_from_cmp[T; Ord[T]](&a: &T, &b: &T) -> Bool {
    a.cmp(&b) is Ord::Lt()
  }

  pub fn le_from_cmp[T; Ord[T]](&a: &T, &b: &T) -> Bool {
    !(a.cmp(&b) is Ord::Gt())
  }

  pub fn cmp_from_lt[T; Ord[T]](&a: &T, &b: &T) -> Ord {
    when {
      a < b { Ord::Lt() }
      a > b { Ord::Gt() }
      _ { Ord::Eq() }
    }
  }
}

pub trait[T] Lt {
  #[builtin = "lt"]
  fn .lt(a: &T, b: &T) -> Bool;
}

pub mod Lt {
  pub impl from_ord[T; Ord[T]]: Lt[T] {
    fn lt(a: &T, b: &T) -> Bool {
      Ord::lt(a, b)
    }
  }

  #[builtin = "gt"]
  pub fn .gt[T; Lt[T]](&a: &T, &b: &T) -> Bool {
    b < a
  }

  #[basic]
  pub impl struct_[S, C; Struct[S, C], Lt[C]]: Lt[S] {
    fn lt(&a: &S, &b: &S) -> Bool {
      Lt::lt(&a as &C, &b as &C)
    }
  }
}

pub trait[T] Le {
  #[builtin = "le"]
  fn .le(a: &T, b: &T) -> Bool;
}

pub mod Le {
  pub impl from_ord[T; Ord[T]]: Le[T] {
    fn le(a: &T, b: &T) -> Bool {
      Ord::le(a, b)
    }
  }

  #[builtin = "ge"]
  pub fn .ge[T; Le[T]](&a: &T, &b: &T) -> Bool {
    b <= a
  }

  #[basic]
  pub impl struct_[S, C; Struct[S, C], Le[C]]: Le[S] {
    fn le(&a: &S, &b: &S) -> Bool {
      Le::le(&a as &C, &b as &C)
    }
  }
}
