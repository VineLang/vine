
pub type A;

pub mod A {
  pub impl : Drop[A] = unsafe::erase;
  pub impl : Show[A];

  pub fn new() -> A {
    unsafe::eraser
  }
}

pub fn main(&io: &IO) {
  let a = A::new();

  io.println("a: {a.show()}");
}
