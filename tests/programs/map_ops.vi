
use #std::data::Map;

pub fn main(&io: &IO) {
  let map = Map::empty[N32, String];

  map.insert(4, "four");
  map.insert(123456, "one hundred twenty three thousand four hundred fifty six");
  map.insert(1, "one");

  io.println("{map.has(&4)}");
  io.println("{map.has(&42)}");
  io.println("{map.has(&123456)}");

  io.println("{map.show()}")
}
