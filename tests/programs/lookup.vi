
use #root::data::Lookup;

pub fn main(&io: &IO) {
  io.println("{squares.lookup(4)}");
  io.println("{squares.lookup(6)}");
}

fn squares() -> Lookup::Size512[N32] {
  Lookup((0..512).map(fn* (x: N32) { x * x }).collect[List]())
}
