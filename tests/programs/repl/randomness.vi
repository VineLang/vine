use std::rng::Pcg32;
let rng = Pcg32::default
let nums: List[N32] = []
let len = 20
while len != 0 { len -= 1; nums ++= [rng.random[_, N32]() % 100] }
let str = ""
let len = 100
while len != 0 { len -= 1; str ++= String([' ' + rng.random[_, N32]() % 95]) }
