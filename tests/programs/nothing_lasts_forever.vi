
pub fn main(&io: &IO) {
  let i = 0;
  while io.forever() {
    io.println("{i}");
    if i == 46 {
      // since `IO::forever` waits on `io` to continue the loop, this will stop the loop early
      unsafe::erase(&io);
    }
    i += 1;
  }
}
