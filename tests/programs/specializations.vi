
pub fn main(&io: &IO) {
  io.println([1, 2, 3, 4].to_string[N32]());
  io.println(["abc", "def", "ghi"].to_string[String]());
  io.println(['x', 'y', 'z'].to_string[Char]());
  io.println([true, false].to_string[Bool]());
  io.println([(1, 'a'), (2, 'b')].to_string[(N32, Char)]());
}
