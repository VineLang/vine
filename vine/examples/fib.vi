
const end: u32 = 32;

fn main(&io: &IO) {
  let n = 0;
  let a = 0;
  let b = 1;
  while n <= end {
    io.println("fib(" ++ n.to_string() ++ ") = " ++ a.to_string());
    (a, b) = (b, a + b);
    n += 1;
  }
}
