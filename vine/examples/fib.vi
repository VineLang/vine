
use std::{io::println, u32, hint::black_box};

const end = black_box(32);

fn main(io) {
  let n = 0;
  let a = 0;
  let b = 1;
  while n <= end {
    io.println("fib(" ++ u32::to_string(n) ++ ") = " ++ u32::to_string(a));
    (a, b) = (b, a + b);
    n += 1;
  }
}
