
use std::{io::println, u32};

fn main(io) {
  io.println(loop_unit());

  // TODO: how to print tuples?
  let u = unit();
  io.println(u);
}

fn unit() {
  return;
}

fn loop_unit() {
  return loop {
    break "hey";
  }
}