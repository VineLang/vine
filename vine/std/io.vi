
fn println(io, str) {
  let io = print(io, str);
  let io = ::ext::io_print_char(io, '\n');
  io
}

fn print(io, str) {
  let (len, buf) = str;
  let chars = buf(_);
  let char;
  while len {
    len = len - 1;
    (char, chars) = chars;
    io = ::ext::io_print_char(io, char);
  }
  io
}
